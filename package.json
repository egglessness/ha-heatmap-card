{
  "scripts": {
    "prebuild": "python3 build/rollup_scales.py",
    "build": "esbuild src/heatmap-card-anchor.js --loader:.json=json --bundle --outfile=heatmap-card.js --keep-names"
  },
  "dependencies": {
    "esbuild": "^0.17.18"
  }
}
